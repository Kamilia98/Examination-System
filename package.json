{
  "name": "examination-system",
  "version": "1.0.0",
  "description": "Examination System",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx json-server --watch db/db.json --port 5000"
  },
  "author": "Kamilia",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "json-server": "^0.17.3"
  }
}
